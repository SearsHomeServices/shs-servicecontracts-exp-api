<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:json-logger="http://www.mulesoft.org/schema/mule/json-logger"
    xmlns:tls="http://www.mulesoft.org/schema/mule/tls"
    xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
    xmlns:http="http://www.mulesoft.org/schema/mule/http"
    xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
    xmlns="http://www.mulesoft.org/schema/mule/core"
    xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd 
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/tls http://www.mulesoft.org/schema/mule/tls/current/mule-tls.xsd
        http://www.mulesoft.org/schema/mule/json-logger http://www.mulesoft.org/schema/mule/json-logger/current/mule-json-logger.xsd">

    <!-- Non-secure properties -->
    <configuration-properties
        doc:name="Properties Config"
        file="properties/${mule.env}.yaml" />

    <!-- Secure properties -->
    <secure-properties:config 
        name="Secure_Properties_Config"
        doc:name="Secure Properties Config"
        file="properties/${mule.env}-secure.yaml"
        key="${mule.vault.key}" />

    <!-- HTTP Requester -->
    <http:request-config
        name="notification-http-request-config"
        doc:name="HTTP Request configuration"
        responseTimeout="${http.timeouts.response}"
        basePath="${notifications.basePath}">
        <http:request-connection
            host="${notifications.host}"
            port="${notifications.port}"
            connectionIdleTimeout="${http.timeouts.connection}"
            protocol="HTTPS">
            <reconnection >
                <reconnect 
                    frequency="${reconnection.frequency}" 
                    count="${reconnection.attempts}" />
            </reconnection>
            <http:authentication >
                <http:basic-authentication 
                    username="${secure::api.client_id}" 
                    password="${secure::api.client_secret}" />
            </http:authentication>
        </http:request-connection>
    </http:request-config>
 <http:request-config name="HTTP_Request_configuration_parts-proxy" doc:name="HTTP Request configuration" doc:id="3ab716dd-c226-431a-96f6-eea86f2c07d4" basePath="${parts-proxy.basePath}">
		<http:request-connection host="${parts-proxy.host}" port="${parts-proxy.port}" protocol="HTTPS" tlsContext="TrustStore">
			<reconnection >
				<reconnect count="3" />
			</reconnection>
		</http:request-connection>
	</http:request-config>    

    <tls:context 
        name="Keystore" 
        doc:name="TLS Context" 
        doc:id="368ff486-ff61-495d-bf67-8b6f5cceedde" >
        <tls:key-store 
            type="jks" 
            path="${key.path}" 
            alias="${key.alias}" 
            keyPassword="${secure::keystore.keyPwd}" 
            password="${secure::keystore.pwd}" />
    </tls:context>

    <tls:context 
        name="TrustStore" 
        doc:name="TLS Context" 
        doc:id="d514ca4b-a199-43db-86d2-0f60bd30ee1f" >
        <tls:trust-store 
            path="${trust.path}" 
            password="${secure::truststore.pwd}" 
            type="jks" />
    </tls:context>

    <json-logger:config 
        name="JsonLoggerConfig" 
        doc:name="Json Logger Config" 
        disabledFields="${json.logger.disabledFields}" />

</mule>
